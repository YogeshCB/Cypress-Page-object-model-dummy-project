/* TODO : Add operations for firebase persistence
import actions from './actions';
import services from '../../../services';

const approveAsset = value => dispatch => {
    // Add in firebase & dispact action
};

const deselectAsset = value => dispatch => {
    // Add in firebase & dispact action
};

export default {
    approveAsset,
    deselectAsset
}
*/
