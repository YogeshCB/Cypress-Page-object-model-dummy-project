import { getTypes } from "@bit/kubric.redux.reducks.utils";

export default getTypes([
  'NAV_SELECTED',
  'RECENT_CAMPAIGNS_UPDATED',
  'RECENT_ASSETS_UPDATED',
  'APP_LOADED',
  'UPDATE_SELECTED_NAV'
], 'kubric/app');