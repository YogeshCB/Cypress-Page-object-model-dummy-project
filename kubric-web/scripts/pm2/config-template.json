{
  "apps": [
    {
      "name": "{{DEPLOYMENT_DIRECTORY}}",
      "interpreter": "/home/deploy/.nvm/versions/node/v10.16.0/bin/node",
      "script": "./dist/bin/www",
      "cwd": "/home/deploy/{{CURRENT_DIRECTORY}}",
      "env": {
        "NODE_ENV": "{{BUILD_TYPE}}",
        "{{PORT_TYPE}}": "{{PORT}}"
      }
    }
  ]
}